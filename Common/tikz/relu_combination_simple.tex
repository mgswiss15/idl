% Common/tikz/relu_combination_simple.tex
\begin{tikzpicture}[scale=0.85]
\tikzstyle{every node}=[font=\scriptsize]
\tikzstyle{neuron} = [circle, draw, fill=thwsorange!20, minimum size=0.6cm, outer sep=3pt]
\tikzstyle{input} = [circle, draw, fill=thwsblue!15, minimum size=0.6cm, outer sep=3pt]
\tikzstyle{conn} = [thick, -Stealth, color=thwsblue]
\tikzstyle{outarr} = [thick, -Stealth]

% Input nodes
\node[input] (x) at (-2, -0.5) {$x$};

% Neuron
\node[neuron] (n1) at (0, 0) {$\sigma$};
\node[neuron] (n2) at (0, -1) {$\sigma$};

% Connections
\draw[conn] (x) -- (n1);

% Connections
\draw[conn] (x) -- (n2);

% Output neuron
\node[neuron] (outn) at (2, -0.5) {$\Sigma$};

% Connections
\draw[conn] (n1) -- (outn);
\draw[conn] (n2) -- (outn);

% Output
\draw[outarr] (outn) -- ++(1.5, 0) node[right] {$\yhat$};

\end{tikzpicture}