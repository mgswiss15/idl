% Figure C: Rolled RNN cell — explicit labels, output shown
\begin{tikzpicture}[
  node distance = 1.4cm and 2.0cm,
  hnode/.style = {draw, rounded corners=4pt, minimum width=1.1cm, minimum height=0.7cm,
                  fill=thwsblue!15, draw=thwsblue, thick, font=\small},
  cell/.style  = {draw, rounded corners=6pt, minimum width=1.2cm, minimum height=0.9cm,
                  fill=thwspetrol!15, draw=thwspetrol, thick, font=\normalsize},
  xnode/.style = {draw, circle, minimum size=0.7cm,
                  fill=thwsorange!25, draw=thwsorange!80!black, thick, font=\small},
  ynode/.style = {draw, circle, minimum size=0.7cm,
                  fill=thwsgrey!40, draw=thwsgrey!80, thick, font=\small},
  arr/.style   = {->, >=stealth, thick},
  lbl/.style   = {font=\scriptsize, midway},
]

% Nodes
\node[hnode] (hprev) {$\mathbf{h}^{(t-1)}$};
\node[cell,  right=1.3cm of hprev] (cell) {$\tanh$};
\node[hnode, right=1.3cm of cell]  (hnext) {$\mathbf{h}^{(t)}$};
\node[xnode, below=1.0cm of cell]  (xt)   {$\mathbf{x}^{(t)}$};
\node[ynode, above=1.0cm of cell]  (yt)   {$\mathbf{y}^{(t)}$};

% Arrows with weight labels
\draw[arr] (hprev) -- node[above, lbl, color=carnelian] {$\mathbf{W}_h$} (cell);
\draw[arr] (xt)    -- node[right, lbl, color=thwsorange!80!black] {$\mathbf{W}_x$} (cell);
\draw[arr] (cell)  -- node[above, lbl] {} (hnext);
\draw[arr] (cell)  -- node[right, lbl, color=black!50] {$\mathbf{W}_y$} (yt);

% Self-loop label
\node[font=\scriptsize, color=carnelian, below right=0.05cm and 0.1cm of hprev]
     {recurrent};

% Equation annotation
\node[font=\scriptsize, align=left, anchor=west] at (5.2cm, 0)
  {$\mathbf{h}^{(t)} = \tanh\!\bigl(\mathbf{W}_h\,\mathbf{h}^{(t-1)} + \mathbf{W}_x\,\mathbf{x}^{(t)} + \mathbf{b}\bigr)$\\[0.5em]
   $\mathbf{y}^{(t)} = \mathbf{W}_y\,\mathbf{h}^{(t)}$};

\end{tikzpicture}
