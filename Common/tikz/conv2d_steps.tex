% conv2d_steps.tex — three filter positions on same input
\begin{tikzpicture}[scale=0.72]

% --- position 1: top-left (patch at col 0-2, row 2-4 in bottom-up coords) ---
\begin{scope}[xshift=0cm]
    % full grid
    \foreach \col in {0,...,4} {
        \foreach \row in {0,...,4} {
            \draw[thwspetrol!25, fill=thwspetrol!7]
                (\col*0.52, \row*0.52) rectangle (\col*0.52+0.48, \row*0.52+0.48);
        }
    }
    % patch cols 0-2, rows 2-4 (bottom-up: rows 0-2)
    \foreach \col in {0,1,2} {
        \foreach \row in {2,3,4} {
            \draw[thwsorange, fill=thwsorange!30, thick]
                (\col*0.52, \row*0.52) rectangle (\col*0.52+0.48, \row*0.52+0.48);
        }
    }
    % output grid 3x3
    \foreach \col in {0,1,2} {
        \foreach \row in {0,1,2} {
            \draw[carnelian!30, fill=carnelian!8]
                (\col*0.52+2.9, \row*0.52+0.52) rectangle
                (\col*0.52+3.38, \row*0.52+1.0);
        }
    }
    % highlight top-left output
    \draw[carnelian, fill=carnelian!40, thick]
        (2.9, 1.54) rectangle (3.38, 2.02);
    \node[font=\scriptsize, thwsorange] at (2.7,-0.3) {step 1};

\draw[->, thick, thwsgrey] (4.7,1.3) -- (5.5,1.3);

\end{scope}

% --- position 2: shifted right (patch at col 1-3, row 2-4) ---
\begin{scope}[xshift=6cm]
    \foreach \col in {0,...,4} {
        \foreach \row in {0,...,4} {
            \draw[thwspetrol!25, fill=thwspetrol!7]
                (\col*0.52, \row*0.52) rectangle (\col*0.52+0.48, \row*0.52+0.48);
        }
    }
    \foreach \col in {1,2,3} {
        \foreach \row in {2,3,4} {
            \draw[thwsorange, fill=thwsorange!30, thick]
                (\col*0.52, \row*0.52) rectangle (\col*0.52+0.48, \row*0.52+0.48);
        }
    }
    \foreach \col in {0,1,2} {
        \foreach \row in {0,1,2} {
            \draw[carnelian!30, fill=carnelian!8]
                (\col*0.52+2.9, \row*0.52+0.52) rectangle
                (\col*0.52+3.38, \row*0.52+1.0);
        }
    }
    % highlight second output in top row
    \draw[carnelian, fill=carnelian!40, thick]
        (3.42, 1.54) rectangle (3.9, 2.02);
    \node[font=\scriptsize, thwsorange] at (2.7,-0.3) {step 2};

\draw[->, thick, thwsgrey] (4.7,1.3) -- (5.5,1.3);
\end{scope}

% --- position 3: shifted down (patch at col 0-2, row 1-3) ---
\begin{scope}[xshift=12cm]
    \foreach \col in {0,...,4} {
        \foreach \row in {0,...,4} {
            \draw[thwspetrol!25, fill=thwspetrol!7]
                (\col*0.52, \row*0.52) rectangle (\col*0.52+0.48, \row*0.52+0.48);
        }
    }
    \foreach \col in {0,1,2} {
        \foreach \row in {1,2,3} {
            \draw[thwsorange, fill=thwsorange!30, thick]
                (\col*0.52, \row*0.52) rectangle (\col*0.52+0.48, \row*0.52+0.48);
        }
    }
    \foreach \col in {0,1,2} {
        \foreach \row in {0,1,2} {
            \draw[carnelian!30, fill=carnelian!8]
                (\col*0.52+2.9, \row*0.52+0.52) rectangle
                (\col*0.52+3.38, \row*0.52+1.0);
        }
    }
    % highlight second output in first column
    \draw[carnelian, fill=carnelian!40, thick]
        (2.9, 1.04) rectangle (3.38, 1.52);
    \node[font=\scriptsize, thwsorange] at (2.7,-0.3) {step 3};
\end{scope}

% \node[font=\scriptsize, thwspetrol, align=center] at (8.5,-1)
%     {same filter slides across input --- each position produces one output value};

\end{tikzpicture}
